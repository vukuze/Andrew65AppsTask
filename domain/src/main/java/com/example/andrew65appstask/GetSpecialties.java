package com.andrew.personnel;

//import com.example.andrew65appstask.db.Specialty;

public class GetSpecialties {
//    public void execute(DisposableObserver<List<Specialty>> observer) {
//        Preconditions.checkNotNull(observer);
//        final Observable<T> observable = this.buildUseCaseObservable(params)
//                .subscribeOn(Schedulers.from(threadExecutor))
//                .observeOn(postExecutizonThread.getScheduler());
//        addDisposable(observable.subscribeWith(observer));
//    }
}
